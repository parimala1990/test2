package seleniumCode;

import org.testng.annotations.Test;

public class NewTest {
  @Test
  public void f() {
	  
	  
	  
//		driver.findElement(By.xpath("//span[contains(text(),'Fle')]/ancestor::a")).click();
		 

		 
		 //
		  /*driver.findElement(By.id("textfield-1031-inputEl")).click();
		  
		  
		 //implict wait 
		  driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);
		  
		  //explicit wait 
		  WebDriverWait wait = new WebDriverWait(driver,30);
		  wait.until(ExpectedConditions.presenceOfElementLocated(By.xpath("textfield-1031-inputEl")));
		  driver.findElement(By.xpath("textfield-1031-inputEl"));
		  wait.until(ExpectedConditions.visibilityOfElementLocated(By.xpath("")));
		  wait.until(ExpectedConditions.alertIsPresent());
		  wait.until(ExpectedConditions.frameToBeAvailableAndSwitchToIt(By.xpath(""))); */
  }
}
